<p>
  <code>
  function print_circum(radius) {
  let circum = 2 * 3.14159 * radius;
  console.log("The circumference of a circle with radius " + radius + " is " + circum);
  }
  </code>
</p>

//entradas e saídas para três chamadas da print_circumfunção

<p>
  <code>
  print_circum(5);
  // Output: The circumference of a circle with radius 5 is 31.4159

  print_circum(10);
  // Output: The circumference of a circle with radius 10 is 62.8318

  print_circum(2.5);
  // Output: The circumference of a circle with radius 2.5 is 15.70795
  </code>
</p>

//Na print_circumfunção, primeiro calculamos a circunferência do círculo usando a fórmula 2πr, onde π é aproximado de 3,14159. Em seguida, imprimimos o resultado usando console.log().

Quando chamamos a print_circumfunção com raio 5, a saída é "A circunferência de um círculo com raio 5 é 31,4159". Da mesma forma, quando chamamos a função com raio 10, a saída é "A circunferência de um círculo com raio 10 é 62,8318". Finalmente, quando chamamos a função com raio de 2,5, a saída é "A circunferência de um círculo com raio de 2,5 é 15,70795".
